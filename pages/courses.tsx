import React from 'react';

const CoursesPage = (props: any) => {
    return (
        <div>
            Courses
        </div>
    );
};

CoursesPage.propTypes = {
    
};

export default CoursesPage;