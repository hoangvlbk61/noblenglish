import React from 'react';

const LibraryPage = (props: any) => {
    return (
        <div>
            Library page
        </div>
    );
};

LibraryPage.propTypes = {
    
};

export default LibraryPage;