import React from 'react';

const ContactPage = (props: any) => {
    return (
        <div>
            Contact page
        </div>
    );
};

ContactPage.propTypes = {
    
};

export default ContactPage;